SCHEMA_EVENTS = {
    "listing.created":        "A canonical listing was created",
    "listing.validated":      "A listing passed validation",
    "validation.failed":      "A listing failed validation",
    "schema.migrated":        "A listing was migrated to a newer schema version",
}
